{# Check if either forward_url or reverse_url is not an empty string #}
{% if forward_url or reverse_url %}
    {% if forward_url %}
        track type=bigWig name="{{ Run }} (Forward Strand)" description="{{ description }} (Forward Strand)" bigDataUrl={{ forward_url }}
    {% endif %}
    {% if reverse_url %}
        track type=bigWig name="{{ Run }} (Reverse Strand)" description="{{ description }} (Reverse Strand)" bigDataUrl={{ reverse_url }}
    {% endif %}
{% else %}
    None available
{% endif %}